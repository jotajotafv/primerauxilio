{
  "name": "first-aid-3d-experience",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "test:dist": "node --test tests/dist.test.js",
    "test:deployed": "playwright test tests/browser/production.spec.js",
    "inspect:models": "node scripts/inspect-models.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^7.1.0"
  }
}
